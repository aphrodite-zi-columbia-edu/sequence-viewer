{
  "name": "sequence-viewer-command",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "postinstall": "bower install -f && sed -i '/if ( rpSearch.test( data ) ) {/c\\\t\tif ( ( searchStr === \"*\" && data ) || rpSearch.test( data ) ) {' public/components/datatables.net/js/jquery.dataTables.js"
  },
  "dependencies": {
    "body-parser": "~1.17.1",
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.3",
    "express": "~4.15.2",
    "express-handlebars": "^3.0.0",
    "hbs": "~4.0.1",
    "morgan": "~1.8.1",
    "serve-favicon": "~2.4.2"
  }
}
